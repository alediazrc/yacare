{% if paginar %}
<div class="navigation hidden-print">
	<span class="sr-only">Páginas: </span> {{
	knp_pagination_render(entities,
	'TapirBaseBundle:Form:pagination.html.twig') }}
</div>
{% endif %}
