{% extends 'YacareBaseBundle:Default:listar.html.twig' %}

{% block pagina_titulo %}DesinfeccionVehiculo{% endblock %}



{% block pagina_contenido %}

{% include 'YacareBaseBundle::buscar.html.twig' %}

<table class="table table-striped">
    <thead>
        <tr>            
            <th>Vehiculo</th>
            <th>Fecha</th>
            <th>Comprobante</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a class="btn btn-default btn-xs" href="{{ path(baseroute ~ '_editar', { 'id': entity.Id }) }}"><i class="fa fa-edit"></i> Editar</a></td>
            <td>{{ entity.Vehiculo }}</td>
            <td>{{ entity.FechaDesinfeccionVehiculo|date("d/m/Y") }}</td>  
            <td>{{ entity.id }}</td>
            <td>

        </tr>
    {% endfor %}
    </tbody>
</table>

{% include 'YacareBaseBundle::paginar.html.twig' %}

<a href="{{ path(baseroute ~ '_editar_1', arrastre) }}" class="btn btn-primary"><i class="fa fa-plus"></i> Nueva Desinfección</a>

{% endblock %}
