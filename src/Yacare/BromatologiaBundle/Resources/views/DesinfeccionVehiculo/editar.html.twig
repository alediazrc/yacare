{% extends 'YacareBaseBundle::layout.html.twig' %}

{% block title %}DesinfeccionVehiculo{% endblock %}

{% block content %}

<form class="form-horizontal" action="{{ path('yacare_bromatologia_desinfeccionvehiculo_guardar', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
	<legend>{% if entity.id %} {{ entity.Vehiculo }} {% else %} Nueva Desinfeccion {% endif %}</legend>
	{{ form_widget(edit_form) }}
    <div class="form-actions">
        <button type="submit" class="btn btn-primary"><i class="icon-white icon-ok"></i> Guardar</button>
		<a class="btn" href="{{ path('yacare_bromatologia_desinfeccionvehiculo_listar') }}">Cancelar</a>
    </div>
</form>

{% if entity.id %}
<a class="btn btn-danger" href="{{ path('yacare_bromatologia_desinfeccionvehiculo_eliminar', { 'id': entity.id }) }}"><i class="icon-trash icon-white"></i> Eliminar Desinfeccion</a>
{% endif %}

{% endblock %}