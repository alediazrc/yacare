{% extends 'YacareBaseBundle:Default:listar.html.twig' %}

{% block pagina_titulo %}Libreta{% endblock %}



{% block pagina_contenido %}

{% include 'YacareBaseBundle::buscar.html.twig' %}

<table class="table table-striped">
    <thead>
        <tr>
            <th>Persona</th>
            <th>Fecha certificado</th>
            <th>Profesional</th>
            <th>Curso</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a class="btn btn-default btn-xs" href="{{ path(baseroute ~ '_editar', { 'id': entity.Id }) }}"><i class="fa fa-edit"></i> Editar</a></td>
            <td>{{ entity.Persona }}</td>
            <td>{{ entity.FechaCertificado|date("d/m/Y") }}</td>
            <td>{{ entity.Profesional }}</td> 
            <td>{{ entity.Curso }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>

{% include 'YacareBaseBundle::paginar.html.twig' %}

<a href="{{ path(baseroute ~ '_editar_1', arrastre) }}" class="btn btn-primary"><i class="fa fa-plus"></i> Nueva Libreta</a>

{% endblock %}
