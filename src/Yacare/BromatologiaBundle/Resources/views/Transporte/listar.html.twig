{% extends 'YacareBaseBundle:Default:listar.html.twig' %}

{% block pagina_titulo %}Transporte{% endblock %}



{% block pagina_contenido %}

{% include 'YacareBaseBundle::buscar.html.twig' %}

<table class="table table-striped">
    <thead>
        <tr>  
            <th> </th>
            <th>Nombre</th>
            <th>Titular</th>
            <th>Domicilio</th> 
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a class="btn btn-default btn-xs" href="{{ path(baseroute ~ '_editar', { 'id': entity.Id }) }}"><i class="fa fa-edit"></i> Editar</a></td>
            <td>{{ entity.Nombre }}</td>
            <td>{{ entity.Titular }}</td>
            <td>{{ entity.Domicilio }}</td> 
        </tr>
    {% endfor %}
    </tbody>
</table>

{% include 'YacareBaseBundle::paginar.html.twig' %}

<a href="{{ path(baseroute ~ '_editar_1', arrastre) }}" class="btn btn-primary"><i class="fa fa-plus"></i> Nuevo Transporte</a>

{% endblock %}
