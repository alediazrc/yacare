{% extends 'YacareBaseBundle:Default:listar.html.twig' %}

{% block pagina_titulo %}DesinfeccionLocal{% endblock %}



{% block pagina_contenido %}

{% include 'TapirBaseBundle::buscar.html.twig' %}

<table class="table table-striped">
    <thead>
        <tr>            
            <th>Local</th>
            <th>Titular</th>
            <th>Fecha</th>
            <th>Tipo Desinfeccion</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a data-toggle="ajax-link" class="btn btn-default btn-xs hidden-print" href="{{ path(baseroute ~ '_editar', { 'id': entity.Id }) }}"><i class="fa fa-edit"></i> Editar</a></td>
            <td>{{ entity.Local }}</td>
            <td>{{ entity.Titular }}</td>
            <td>{{ entity.FechaDesinfeccionLocal|date("d/m/Y") }}</td>
            <td>{{ entity.TipoDesinfeccionLocalNombre }}</td>
            <td>
        </tr>
    {% endfor %}
    </tbody>
</table>

{% include 'TapirBaseBundle::paginar.html.twig' %}

<a data-toggle="ajax-link" href="{{ path(baseroute ~ '_editar_1', arrastre) }}" class="btn btn-primary"><i class="fa fa-plus"></i> Nueva Desinfección</a>

{% endblock %}
