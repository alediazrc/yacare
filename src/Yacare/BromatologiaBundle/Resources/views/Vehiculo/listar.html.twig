{% extends 'YacareBaseBundle:Default:listar.html.twig' %}

{% block pagina_titulo %}Vehiculo{% endblock %}



{% block pagina_contenido %}

{% include 'YacareBaseBundle::buscar.html.twig' %}

<table class="table table-striped">
    <thead>
        <tr>
            <th> </th>
            <th>Dominio</th>
            <th>Marca</th>
            <th>Modelo</th>
            <th>Ano</th>
            <th>Peso</th> 
            <th>Transporte</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a data-toggle="ajax-link" class="btn btn-default btn-xs hidden-print" href="{{ path(baseroute ~ '_editar', { 'id': entity.Id }) }}"><i class="fa fa-edit"></i> Editar</a></td>
            <td>{{ entity.Dominio }}</td>
            <td>{{ entity.Marca }}</td>
            <td>{{ entity.Modelo }}</td>
            <td>{{ entity.Ano }}</td>
            <td>{{ entity.Peso }}</td>
            <td>{{ entity.Transporte }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>

{% include 'YacareBaseBundle::paginar.html.twig' %}

<a data-toggle="ajax-link" href="{{ path(baseroute ~ '_editar_1', arrastre) }}" class="btn btn-primary"><i class="fa fa-plus"></i> Nuevo Vehículo</a>

{% endblock %}
