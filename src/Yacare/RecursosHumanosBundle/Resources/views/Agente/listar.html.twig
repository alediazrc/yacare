{% extends 'YacareBaseBundle::layout.html.twig' %}

{% block title %}Agente{% endblock %}

{% block content_header '' %}

{% block content %}
<table class="table table-striped">
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Legajo</th>
            <th>Fecha de ingreso</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><i class="icon-user"></i> <a href="{{ path('yacare_recursoshumanos_agente_editar', { 'id': entity.Id }) }}">{{ entity.NombreVisible }}</a></td>
            <td>{{ entity.Legajo }}</td>
            <td>{{ entity.FechaIngreso|date('d-m-Y') }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<a href="{{ path('yacare_recursoshumanos_agente_editar_1') }}" class="btn"><i class="icon-plus"></i> Alta de nuevo agente</a>
{% endblock %}