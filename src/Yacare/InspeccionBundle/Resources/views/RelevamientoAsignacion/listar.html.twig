{% extends 'YacareBaseBundle::layout.html.twig' %}

{% block pagina_titulo %}Relevamientos - Asignaciones{% endblock %}



{% block pagina_contenido %}
{% if entities is empty %}
<div>
<div class="alert alert-success">
    <button type="button" class="close" data-dismiss="alert">×</button>
    <strong><p>No hay asignaciones.</p></strong>
    <p>Para realizar asignaciones utilice los botones de &quot;Asignar macizo&quot; o &quot;Asignar calle&quot;.
    </p>
</div>
</div>
{% else %}
<table class="table table-striped">
    <thead>
        <tr>
            <th> </th>
            <th>Asignación</th>
            <th>Encargado</th>
            <th>Resultados</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td>
                <a class="btn btn-default btn-xs" href="{{ path('yacare_inspeccion_relevamientoasignacionresultado_listarrelevamiento', { 'id': entity.Relevamiento.Id, 'filtro_asignacion': entity.id }) }}"><i class="fa fa-list"></i> Resultados</a>
            </td>
            <td>{% if entity.Calle %}Calle {{ entity.Calle }}{% else %}Sección {{ entity.Seccion }}, macizo {{ entity.Macizo }}{% endif %}</td>
            <td>{{ entity.Encargado }}</td>
            <td>{% if entity.DetallesCantidad %}{{ entity.DetallesResultadosCantidad }} de {{ entity.DetallesCantidad }}{% else %}-{% endif %}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>

{# display navigation #}
<div class="navigation">
    {{ knp_pagination_render(entities) }}
</div>

{% endif %}
{% endblock %}

{% block pagina_acciones2 %}
<a class="btn btn-primary" href="{{ path('yacare_inspeccion_relevamientoasignacion_asignarmacizo', { 'id': id }) }}"><i class="fa fa-map-marker"></i> Asignar macizo</a>
<a class="btn btn-primary" href="{{ path('yacare_inspeccion_relevamientoasignacion_asignarcalle', { 'id': id }) }}"><i class="fa fa-map-marker"></i> Asignar calle</a>
&nbsp;
&nbsp;
<a href="{{ path('yacare_inspeccion_relevamientoasignacionresultado_listarrelevamiento', { 'id': id }) }}" class="btn btn-primary"><i class="fa fa-list"></i> Ver todos los resultados</a>
&nbsp;
<a href="{{ path('yacare_inspeccion_relevamiento_editar', { 'id': id }) }}" class="btn btn-primary"><i class="fa fa-edit"></i> Editar relevamiento</a>
{% endblock %}