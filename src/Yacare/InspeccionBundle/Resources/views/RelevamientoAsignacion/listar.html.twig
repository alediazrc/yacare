{% extends 'YacareBaseBundle::layout.html.twig' %}

{% block title %}Relevamientos - Asignaciones{% endblock %}

{% block content_header '' %}

{% block content %}
<table class="table table-striped bootstrap-datatable datatable">
    <thead>
        <tr>
            <th>Nombre</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><i class="icon-hdd"></i> <a href="{{ path('yacare_inspeccion_relevamientoasignacion_editar', { 'id': entity.Id }) }}">{{ entity.Nombre }}</a></td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<a href="{{ path('yacare_inspeccion_relevamientoasignacion_editar_1') }}" class="btn"><i class="icon-plus"></i> Nueva asignación</a>
{% endblock %}