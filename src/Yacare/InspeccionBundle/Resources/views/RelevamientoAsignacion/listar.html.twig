{% extends 'YacareBaseBundle::layout.html.twig' %}

{% block title %}Relevamientos - Asignaciones{% endblock %}

{% block content_header '' %}

{% block content %}
<table class="table table-striped bootstrap-datatable datatable">
    <thead>
        <tr>
            <th>Nombre</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('yacare_inspeccion_relevamientoasignacion_editar', { 'id': entity.Id }) }}">{{ entity.Nombre }}</a></td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<a href="{{ path('yacare_inspeccion_relevamientoasignacion_editar_1', { 'relevamiento': id }) }}" class="btn"><i class="icon-plus"></i> Nueva asignación</a>
<a href="{{ path('yacare_inspeccion_relevamiento_editar', { 'id': id }) }}" class="btn"><i class="icon icon-reply"></i> Volver al relevamiento</a>
{% endblock %}