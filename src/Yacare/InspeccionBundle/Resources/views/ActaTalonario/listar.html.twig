{% extends 'YacareBaseBundle:Default:listar.html.twig' %}

{% block title %}Talonarios de actas{% endblock %}

{% block content_header '' %}

{% block content %}

{% include 'YacareBaseBundle::buscar.html.twig' %}

<table class="table">
    <thead>
        <tr>
            <th> </th>
            <th>Tipo</th>
            <th>Numeracion</th>
            <th>En poder de</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a class="btn btn-mini" href="{{ path(baseroute ~ '_editar', { 'id': entity.Id }) }}"><i class="icon-edit"></i> Editar</a></td>
            <td>{{ entity.Tipo }}</td>
            <td>{{ entity.NumeroDesde }} - {{ entity.NumeroHasta }}</td>
            <td>{{ entity.EnPoderDe }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>

{% include 'YacareBaseBundle::paginar.html.twig' %}

<a href="{{ path(baseroute ~ '_editar_1', arrastre) }}" class="btn"><i class="icon-plus"></i> Nuevo talonario de actas</a>
{% endblock %}
