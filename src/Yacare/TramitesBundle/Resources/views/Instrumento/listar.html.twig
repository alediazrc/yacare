{% extends 'TapirBaseBundle:Default:listar.html.twig' %}

{% block pagina_titulo %}Instrumentos{% endblock %}

{% block pagina_contenido %}
{% include 'TapirBaseBundle::buscar.html.twig' %}

<table class="table table-hover">
    <thead>
        <tr>
            <th> </th>
            <th>Código</th>
            <th>Nombre</th>
            <th class="hidden-sm hidden-xs">Tipo</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a data-toggle="ajax-link" class="btn btn-default btn-xs hidden-print showonhover" href="{{ path(baseroute ~ '_editar', arrastre|merge({ 'id': entity.Id })) }}"><i class="fa fa-edit"></i> Editar</a></td>
            <td>{{ entity.Codigo }}</td>
            <td>&nbsp;{{ entity.Nombre }}</td>
            <td class="hidden-sm hidden-xs">{{ entity.TipoNombre }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>

{% include 'TapirBaseBundle::paginar.html.twig' %}

{% endblock %}

{% block pagina_acciones2 %}
<a data-toggle="ajax-link" href="{{ path(baseroute ~ '_editar_1', arrastre) }}" class="btn btn-primary"><i class="fa fa-plus"></i> Nuevo instrumento</a>
{% endblock %}
