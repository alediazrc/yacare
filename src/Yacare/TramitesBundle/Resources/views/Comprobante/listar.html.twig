{% extends 'YacareBaseBundle:Default:listar.html.twig' %}

{% block pagina_titulo %}Comprobantes{% endblock %}

{% block pagina_contenido %}
{% include 'YacareBaseBundle::buscar.html.twig' %}

<table class="table table-hover">
    <thead>
        <tr>
            <th></th>
            <th>#</th>
            <th>Titular</th>
            <th>Fecha de emisión</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a class="btn btn-default btn-xs hidden-print" href="{{ path(baseroute ~ '_ver', { 'id': entity.Id }) }}"><i class="fa fa-edit"></i> Ver</a></td>
            <td>{{ entity.Numero }}</td>
            <td>{{ entity.Titular }}</td>
            <td>{{ entity.createdAt|localizeddate('full', 'none') }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>

{% include 'YacareBaseBundle::paginar.html.twig' %}
{% endblock %}