{% extends 'YacareBaseBundle:Default:listar.html.twig' %}

{% block pagina_titulo %}Veterinario{% endblock %}



{% block pagina_contenido %}

{% include 'TapirBaseBundle::buscar.html.twig' %}

<table class="table table-striped">
    <thead>
        <tr>            
            <th>Veterinario</th>
            <th>Matrícula</th>        
            <th>Clinica</th> 
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a data-toggle="ajax-link" href="{{ path('yacare_zoonosis_veterinario_editar', { 'id': entity.Id }) }}"><i class="fa fa-edit"></i></a> {{ entity.Veterinario }}</td>
            <td>{{ entity.Matricula }}</td>
            <td>{{ entity.Clinica }}</td>
            <td>
        </tr>
    {% endfor %}
    </tbody>
</table>

{# display navigation #}
<div class="navigation">
    {{ knp_pagination_render(entities) }}
</div>

<a data-toggle="ajax-link" href="{{ path('yacare_zoonosis_veterinario_editar_1') }}" class="btn btn-primary"><i class="fa fa-plus"></i> Nuevo veterinario</a>
{% endblock %}
