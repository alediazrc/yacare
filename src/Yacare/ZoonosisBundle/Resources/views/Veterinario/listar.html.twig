{% extends 'YacareBaseBundle:Default:listar.html.twig' %}

{% block title %}Veterinario{% endblock %}

{% block content_header '' %}

{% block content %}

{% include 'YacareBaseBundle::buscar.html.twig' %}

<table class="table table-striped">
    <thead>
        <tr>            
            <th>Veterinario</th>
            <th>Matrícula</th>        
            <th>Clinica</th> 
            <th>Domicilio</th> 
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('yacare_zoonosis_veterinario_editar', { 'id': entity.Id }) }}"><i class="icon-edit"></i></a> {{ entity.Veterinario }}</td>
            <td>{{ entity.Matricula }}</td>
            <td>{{ entity.Clinica }}</td>
            <td>{{ entity.Domicilio }}</td>
            <td>
        </tr>
    {% endfor %}
    </tbody>
</table>

{# display navigation #}
<div class="navigation">
    {{ knp_pagination_render(entities) }}
</div>

<a href="{{ path('yacare_zoonosis_veterinario_editar_1') }}" class="btn"><i class="icon-plus"></i> Nuevo veterinario</a>
{% endblock %}
