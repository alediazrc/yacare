{% extends 'YacareBaseBundle:Default:listar.html.twig' %}

{% block title %}Zonas{% endblock %}

{% block content_header '' %}

{% block content %}

{% include 'YacareBaseBundle::buscar.html.twig' %}

<table class="table">
    <thead>
        <tr>
            <th> </th>
            <th>Código</th>
            <th>Nombre</th>
            <th>FOS</th>
            <th>FOT</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a class="btn btn-small" href="{{ path(baseroute ~ '_editar', { 'id': entity.id }) }}"><i class="icon-edit"></i> Editar</a></td>
            <td>{{ entity.Codigo }}</td>
            <td>{{ entity.Nombre }}</td>
            <td>{{ entity.Fos|number_format }}</td>
            <td>{{ entity.Fot|number_format }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>

{% include 'YacareBaseBundle::paginar.html.twig' %}

<a href="{{ path(baseroute ~ '_editar_1') }}" class="btn"><i class="icon-plus"></i> Nueva zona</a>

{% endblock %}