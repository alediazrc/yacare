{% extends 'YacareBaseBundle:Default:listar.html.twig' %}

{% block title %}Zonas{% endblock %}

{% block content_header '' %}

{% block content %}

<legend>Zonas</legend>
{% include 'YacareBaseBundle::buscar.html.twig' %}

<table class="table table-hover">
    <thead>
        <tr>
            <th> </th>
            <th>Código</th>
            <th>Nombre</th>
            <th>FOS</th>
            <th>FOT</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a class="btn btn-default btn-xs" href="{{ path(baseroute ~ '_editar', { 'id': entity.id }) }}"><i class="fa fa-edit"></i> Editar</a></td>
            <td>{{ entity.Codigo }}</td>
            <td>{{ entity.Nombre }}</td>
            <td>{{ entity.Fos|number_format }}</td>
            <td>{{ entity.Fot|number_format }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>

{% include 'YacareBaseBundle::paginar.html.twig' %}

<a href="{{ path(baseroute ~ '_editar_1') }}" class="btn btn-primary"><i class="fa fa-plus"></i> Nueva zona</a>

{% endblock %}