{% extends 'YacareBaseBundle::layout.html.twig' %}

{% block title %}Departamentos{% endblock %}

{% block content_header '' %}

{% block content %}

<legend>Departamentos</legend>
{% include 'YacareBaseBundle::buscar.html.twig' %}

<table class="table table-hover">
    <thead>
        <tr>
            <th> </th>
            <th>Nombre</th>
            <th>Rango</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a class="btn btn-default btn-xs" href="{{ path(baseroute ~ '_editar', arrastre|merge({ 'id': entity.Id })) }}"><i class="fa fa-edit"></i> Editar</a></td>
            <td>{{ entity.Nombre }}</td>
            <td>{{ entity.RangoNombre }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>

{% include 'YacareBaseBundle::paginar.html.twig' %}

<a href="{{ path('yacare_organizacion_departamento_editar_1') }}" class="btn btn-primary"><i class="fa fa-plus"></i> Nuevo departamento</a>
{% endblock %}