{% extends 'YacareBaseBundle::layout.html.twig' %}

{% block title %}Dependencias{% endblock %}

{% block content_header '' %}

{% block content %}
<table class="table bootstrap-datatable datatable">
    <thead>
        <tr>
            <th>Nombre</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><i class="icon-hdd"></i> <a href="{{ path('yacare_base_dependencia_editar', { 'id': entity.Id }) }}">{{ entity.Nombre }}</a></td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<a href="{{ path('yacare_base_dependencia_crear') }}" class="btn"><i class="icon-plus"></i> Crear nueva dependencia</a>
{% endblock %}