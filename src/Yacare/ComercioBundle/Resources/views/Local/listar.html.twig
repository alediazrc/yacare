{% extends 'YacareBaseBundle:Default:listar.html.twig' %}

{% block pagina_titulo %}Local{% endblock %}



{% block pagina_contenido %}

<legend>Locales comerciales</legend>
{% include 'YacareBaseBundle::buscar.html.twig' %}

<table class="table table-hover">
    <thead>
        <tr>
            <th></th>
            <th>Domicilio</th>
            <th>Tipo</th>
            <th>Sup.</th>
            <th>Titular</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a class="btn btn-default btn-xs hidden-print" href="{{ path(baseroute ~ '_editar', { 'id': entity.Id }) }}"><i class="fa fa-edit"></i> Editar</a></td>
            <td>{{ entity.Partida.Domicilio }}</td>
            <td>{{ entity.Tipo }}</td>
            <td>{{ entity.Superficie }} m²</td>
            <td>{{ entity.Partida.Titular }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>

{% include 'YacareBaseBundle::paginar.html.twig' %}

<a href="{{ path(baseroute ~ '_editar_1') }}" class="btn btn-primary hidden-print"><i class="fa fa-plus"></i> Nuevo local comercial</a>

{% endblock %}
