{% extends 'TapirBaseBundle::blank.html.twig' %}

{% block pagina_titulo %}Requerimiento anónimo{% endblock %}

{% block pagina_contenido %}
<style>
    .wr {
        padding: 40px !important;
    }
    textarea {
    	min-height: 80px;
    }
</style>
<div class="wr">


{% if entity is defined and entity %}

    <h1><i class="fa fa-ticket"></i> Estado del requerimiento</h1>
    
    <p class="lead text-primary">
    
    </p>
    
    {% if entity.Estado == 0 %}{# Nuevo #}

        <p>Su solicitud será atendida por la mesa de entradas y de ser necesario será derivada al área correspondiente.
        	Si aun no lo hizo, por favor tome nota del número de seguimiento para su solicitud: <strong>{{ entity.id }}-{{ entity.Token }}</strong>.</p>

	{% elseif entity.Estado == 10 %}{# Iniciado #}
	
		<p>Su solicitud está siendo evaluado.</p>

    {% endif %}
    
    <p>La solicitud fue ingresada {{ entity.createdAt|tapir_hacetiempo }}.</p>
    
    <p>La dirección de la página para consultas es la siguiente: 
    	<a href="http://www.riogrande.gob.ar/requerimientos/requerimiento/anonimo/ver/{{ entity.id }}-{{ entity.Token }}">http://www.riogrande.gob.ar/requerimientos/requerimiento/anonimo/ver/{{ entity.id }}-{{ entity.Token }}</a>.
        {% if entity.UsuarioEmail %}
        	Además, se envia copia de las novedades a la dirección de correo electrónico proporcionada.
        {% endif %}	
    </p>

{% else %}

	{% set seg = app.request.query.get('seg') %}

	{% if seg is defined and seg %}
		<h1><i class="fa fa-ticket"></i> No se puede tener acceso al requerimiento</h1>
	{% else %}
		<h1><i class="fa fa-ticket"></i> Consultar estado de un requerimiento</h1>
	{% endif %}
	
	<p class="lead">Para consultar el estado de su consulta, por favor escriba el número de seguimiento a continuación:</p>
	
	<form method="GET" class="form-inline">
		<input class="form-control" type="text" size="12" name="seg" value="{{ seg }}" style="width: 160px" autofocus>
		<button class="btn btn-success">Consultar <i class="fa fa-play"></i></button> 
	</form>

{% endif %}

</div>
{% endblock %}
